<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.qc.seclass.glm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GLM">
        <activity android:name=".EditListName"></activity>
        <activity android:name=".NewList" />
        <activity android:name=".EditQuantity" />
        <activity android:name=".AddItem" />
        <activity android:name=".NewItem" />
        <activity android:name=".ItemList" />
        <activity android:name=".TypeList" />
        <activity android:name=".GroceryItem" />
        <activity android:name=".GroceryList" />
        <activity android:name=".ListHolder" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>